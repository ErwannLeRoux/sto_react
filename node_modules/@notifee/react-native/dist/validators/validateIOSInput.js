"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../utils");
function validateIOSInput(input) {
    const out = {};
    if (!input) {
        return out;
    }
    if (!utils_1.isObject(input)) {
        throw new Error('expected an object value.');
    }
    if (utils_1.hasOwnProperty(input, 'button') && !utils_1.isUndefined(input.button)) {
        if (!utils_1.isString(input.button)) {
            throw new Error("'button' expected a string value.");
        }
        out.button = input.button;
    }
    if (utils_1.hasOwnProperty(input, 'placeholder') && !utils_1.isUndefined(input.placeholder)) {
        if (!utils_1.isString(input.placeholder)) {
            throw new Error("'placeholder' expected a string value.");
        }
        out.placeholder = input.placeholder;
    }
    return out;
}
exports.default = validateIOSInput;
//# sourceMappingURL=validateIOSInput.js.map